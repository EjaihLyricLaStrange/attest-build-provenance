name: GitHub Sigstore Prober

on:
  workflow_dispatch:
  schedule:
    # run every 100 days, as allows
    - cron: '*/5 * * * *'

jobs:
  prober:
    if: github.repository_owner == 'actions'
    permissions:
      attestations: write
      id-token: write
    secrets: inherit
    uses: ./.github/workflows/prober.yaml
    with:
      sigstore: github
